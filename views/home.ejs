<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= restuarant.name%></title>
</head>
<body>
    <%- include('./partials/nav') %>

    <h1>Home Page</h1>

    <% let isOpen = false %>

    <% if (!isOpen) { %>
    <h2>We are open !</h2>
    <% } else { %>
    <h2>We are closed.</h2>
    <% } %>

    <ul>
    <li><%= restuarant.name %></li>
    <li><%= restuarant.address %></li>
    <li><%= restuarant.phone %></li>
    </ul>

</body>
</html>
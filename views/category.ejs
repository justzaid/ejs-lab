<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= category[0].toUpperCase() + category.slice(1) %></title>
</head>
<body>
    <%- include('./partials/nav') %>

    <h1><%= category[0].toUpperCase() + category.slice(1) %></h1>

  <ul>
    <% menuItems.forEach((item) => { %>
      <h3><%=item.name%></h3>
      <li><p>Price: <%=item.price%></p></li>
      <li><p>Rating: <%=item.rating%></p></li>
      <li><p>Details: <%=item.details%></p></li>
    <% }) %>
  </ul>
</body>
</html>